# SQLite just needs a file path — no database server to install or run.
DATABASE_URL="file:./dev.db"

# Signs login tokens. Any long random string works for learning.
# In a real app this must be secret and different per environment.
JWT_SECRET="your-secret-here"

PORT=3300
